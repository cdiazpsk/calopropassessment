import {NextResponse} from 'next/server';import OpenAI from 'openai';import {serviceSupabase} from '../../../../lib/supabase';export async function POST(req,{params}){try{if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:'Missing OPENAI_API_KEY.'},{status:500});const p=await params;const s=serviceSupabase();const{data:a,error}=await s.from('assessments').select('*').eq('id',p.id).single();if(error||!a)return NextResponse.json({error:'Assessment not found.'},{status:404});const openai=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const prompt=`Analyze this hotel property assessment. Return only JSON with summary, condition_score, estimated_rooms_per_day, recommended_program, recommended_pricing, recommended_addons, risks, questions, proposal_scope. Assessment: ${JSON.stringify(a)}`;const r=await openai.chat.completions.create({model:'gpt-4.1-mini',messages:[{role:'user',content:prompt}],temperature:.2,response_format:{type:'json_object'}});const result=JSON.parse(r.choices[0].message.content);await s.from('assessments').update({ai_summary:result.summary,ai_condition_score:result.condition_score,ai_estimated_rooms_per_day:result.estimated_rooms_per_day,ai_recommended_program:result.recommended_program,ai_recommended_pricing:result.recommended_pricing,ai_recommended_addons:result.recommended_addons,ai_risks:result.risks,ai_questions:result.questions,ai_proposal_scope:result.proposal_scope}).eq('id',p.id);return NextResponse.json({ok:true,result})}catch(e){return NextResponse.json({error:e.message||'AI assessment failed.'},{status:500})}}