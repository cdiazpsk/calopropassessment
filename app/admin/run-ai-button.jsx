'use client';import {useState} from 'react';export default function RunAiButton({assessmentId}){const[status,setStatus]=useState('');async function runAi(){setStatus('Running AI assessment...');const res=await fetch(`/api/ai-assessment/${assessmentId}`,{method:'POST'});const json=await res.json();if(!res.ok){setStatus(json.error||'AI assessment failed.');return}setStatus('AI assessment completed. Refreshing...');window.location.reload()}return <div><button className="btn-primary" onClick={runAi}>Run AI Assessment</button>{status&&<p style={{fontWeight:700}}>{status}</p>}</div>}
