import Link from 'next/link';import Header from '../../components/Header';import {serviceSupabase} from '../../lib/supabase';export const dynamic='force-dynamic';export const revalidate=0;async function getAssessment(id){const s=serviceSupabase();const{data}=await s.from('assessments').select('*').eq('id',id).single();return data}export default async function ProposalPage({searchParams}){const sp=await searchParams;const a=sp?.id?await getAssessment(sp.id):null;if(!a)return <><Header/><main style={{padding:30}}>Assessment not found.</main></>;const price=a.ai_recommended_pricing||a.recommended_pricing||'Pricing to be confirmed',program=a.ai_recommended_program||a.recommended_program||'Property Assessment',prod=a.ai_estimated_rooms_per_day||a.estimated_rooms_per_day||'TBD';return <><Header/><main style={{maxWidth:900,margin:'0 auto',padding:'36px 20px 70px'}}><div style={{marginBottom:24}}><Link href={`/assessment-review?id=${a.id}`} className="btn-secondary">Back to Assessment</Link></div><section className="card" style={{padding:34,background:'white'}}><p className="kicker">Caliber Proposal Draft</p><h1 style={{color:'#0B2F5B',fontSize:40,margin:'8px 0 6px',fontWeight:900}}>{a.property_name}</h1><p className="muted">{a.brand} | {a.property_category} | {a.property_city}, {a.property_state}</p><hr style={{border:'none',borderTop:'1px solid #e2e8f0',margin:'24px 0'}}/><h2 style={{color:'#0B2F5B'}}>Executive Summary</h2><p>Caliber Lodging has reviewed the submitted property assessment for {a.property_name}. Based on the information provided, the recommended program is <strong>{program}</strong>.</p><h2 style={{color:'#0B2F5B'}}>Production Forecast</h2><p>Estimated production cadence: <strong>{prod} rooms per day</strong>.</p><h2 style={{color:'#0B2F5B'}}>Budgetary Pricing</h2><p>Recommended pricing range: <strong>{price}</strong></p></section></main></>}