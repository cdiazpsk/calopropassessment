'use client';
import {useState} from 'react';import Header from '../../../components/Header';
export default function AdminLoginPage(){const[password,setPassword]=useState('');const[status,setStatus]=useState('');async function login(e){e.preventDefault();setStatus('Checking...');const params=new URLSearchParams(window.location.search);const next=params.get('next')||'/admin';const res=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const json=await res.json();if(!res.ok){setStatus(json.error||'Login failed.');return}window.location.href=next}return <><Header/><main style={{maxWidth:520,margin:'0 auto',padding:'70px 20px'}}><section className="card" style={{padding:32}}><p className="kicker">Caliber Admin</p><h1 style={{color:'#0B2F5B',fontSize:34,margin:'8px 0 10px',fontWeight:900}}>Admin Login</h1><p className="muted">Enter the admin password to view submitted property assessments.</p><form onSubmit={login} style={{marginTop:24,display:'grid',gap:14}}><label><span className="label">Password</span><input className="input" type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn-primary" type="submit">Login</button>{status&&<div style={{color:status==='Checking...'?'#64748b':'#b42318',fontWeight:800}}>{status}</div>}</form></section></main></>}
